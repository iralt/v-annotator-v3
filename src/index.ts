import type { App } from 'vue'

export interface VAnnotatorV3Options {
  // ライブラリの設定オプション
}

export const VAnnotatorV3 = {
  install(_app: App, _options?: VAnnotatorV3Options) {
    // プラグインのインストール処理
  }
}

export default VAnnotatorV3